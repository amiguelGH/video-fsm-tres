on:
  push
jobs:
  ReuseableMatrixJobForDeployment:
    strategy:
      matrix:
        target: [dev, stage, prod]
    uses: actions/amiguelGH/video-fsm-tres/.github/workflows/deploy-git-pages.yml@main
    with:
      target: ${{ matrix.target }}
